import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- Primary Color ---
  /// Dark Mountain Green (#475B32)
  /// Used in the word KADORE, gives a natural identity and trust.
  static const Color primary = Color(0xFF475B32);

  // --- Secondary Colors ---
  /// Olive Green (#486B3D)
  /// Usage: parts of the mountain and natural layers.
  static const Color oliveGreen = Color(0xFF486B3D);

  /// Dark Brown (#5F3B1D)
  /// Usage: the word MOUNT and earthly touch.
  static const Color darkBrown = Color(0xFF5F3B1D);

  /// Sandy Gold (#D9BC65)
  /// Usage: the background mountain — gives a sense of luxury and nature.
  static const Color sandyGold = Color(0xFFD9BC65);

  /// Dark Olive Black (#1E1C10)
  /// Usage: shadows and depth.
  static const Color darkOliveBlack = Color(0xFF1E1C10);
}
